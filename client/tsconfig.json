{
  "compilerOptions": {
    "baseUrl": ".", //支持webpack配置alias
    "paths": {//支持webpack配置alias
      "@/*": ["./src/*"]
    },
    "jsx": "react", // 在 .tsx文件里支持JSX
    "target": "es5"
  },
  // 指定一个匹配列表（属于自动指定该路径下的所有ts相关文件）
  "exclude": [
    "node_modules"
  ]
}